# ── Site ─────────────────────────────────────────────────────────────────────
# Canonical origin. Defaults to https://seotecnico.dev.br when unset.
NEXT_PUBLIC_DOMAIN=

# ── Indexing fail-safe ───────────────────────────────────────────────────────
# The site sends noindex/nofollow and "Disallow: /" unless this is exactly
# "true". Set it only in the real production environment on Vercel.
SITE_INDEXABLE=

# ── Analytics ────────────────────────────────────────────────────────────────
# GTM web container id (GTM-XXXXXXX). GTM only mounts when set.
NEXT_PUBLIC_GTM_ID=

# ── AI crawler telemetry ─────────────────────────────────────────────────────
# A SEPARATE GA4 property, never the human one: Measurement Protocol hits
# create users and sessions, and mixing crawlers into G-59LQZ6LR72 would skew
# every engagement metric and every RUM percentile.
# Server-side only — the API secret must NEVER carry the NEXT_PUBLIC_ prefix.
# Both unset ⇒ src/proxy.ts classifies the crawler and sends nothing, so only
# the real production environment reports. See docs/measurement-plan.md.
GA4_CRAWLER_MEASUREMENT_ID=
GA4_CRAWLER_API_SECRET=

# ── Crawler detection experiment ─────────────────────────────────────────────
# Salt secret for net_id (docs/detection-experiment.md §2.3): the /24-or-/48
# hash is salted with SHA-256(secret + "YYYY-MM") so it rotates monthly with
# zero storage. Unset ⇒ net_id is omitted from events (H1 correlation blind).
# Server-side only — NEVER NEXT_PUBLIC_.
NET_ID_SALT_SECRET=

# Response delay (ms) applied ONLY to the robots.txt honeypot route, capped
# at 8000 in code. Unset or 0 ⇒ no delay. See detection-experiment.md §4.3.
LAB_TRAP_DELAY_MS=

# ── Google Search Console ────────────────────────────────────────────────────
# HTML-tag verification token (not needed if the domain property is verified
# via DNS, which is the plan — kept for flexibility).
GOOGLE_SITE_VERIFICATION=

# ── Chrome UX Report API ─────────────────────────────────────────────────────
# Key for the CWV checker tool (CLAUDE.md §5.3). Server-side only: it is read
# in a Route Handler and must NEVER carry the NEXT_PUBLIC_ prefix, or it ends
# up extractable from the client bundle and the quota is anyone's to burn.
# Restrict the key to the Chrome UX Report API alone in the Cloud Console —
# an unrestricted key reaches every API enabled in the same project.
# Real value lives only in Vercel env vars and a local .env.local (§14).
CRUX_API_KEY=
